# 开题报告技术路线图预览

在 Cursor 中：

1. 打开本文件，按 `Ctrl+Shift+V` 打开 Markdown 预览（需已装 **Markdown Preview Mermaid Support**）
2. 或 `Ctrl+Shift+P` → 输入 `Mermaid Editor: Open` 打开独立 Mermaid 编辑器（**Mermaid Editor** 插件）

源码文件：`开题报告_技术路线图.mmd`

```mermaid
%%{init: {
  "theme": "base",
  "themeVariables": {
    "fontFamily": "SimSun, Microsoft YaHei, serif",
    "fontSize": "14px",
    "primaryColor": "#ffffff",
    "primaryTextColor": "#000000",
    "primaryBorderColor": "#000000",
    "lineColor": "#000000",
    "clusterBkg": "#ffffff",
    "clusterBorder": "#666666"
  },
  "flowchart": { "curve": "linear", "padding": 12, "nodeSpacing": 28, "rankSpacing": 40 }
}}%%

flowchart TB
    classDef box fill:#ffffff,stroke:#000000,stroke-width:1px,color:#000000
    classDef endbox fill:#ffffff,stroke:#000000,stroke-width:1.5px,color:#000000

    subgraph S1["文献研究法"]
        direction TB
        A1["问题界定与文献综述"]:::box
        A1 --> B1["研究背景与意义"]:::box
        A1 --> B2["国内外文献综述"]:::box
        A1 --> B3["研究内容与目标"]:::box
    end

    subgraph S2["离散事件仿真法"]
        direction TB
        C1["脱敏数据"]:::box
        C2["Simio六工序DES建模"]:::box
        D1["多级批次链结构"]:::box
        D2["搬运、转入转出与换型约束"]:::box
        C1 --> C2
        C2 --> D1
        C2 --> D2
    end

    subgraph S3["仿真优化实验设计法"]
        direction TB
        E1["仿真绩效指标体系构建"]:::box
        E2["Scenario Generator<br/>批次因子网格扫描"]:::box
        E3["基准情景验证"]:::box
        E4["较优因子识别"]:::box
        E1 --> E2 --> E3 --> E4
    end

    subgraph S4["对比实验与敏感性分析法"]
        direction TB
        F1["订单释放与派工规则对比"]:::box
        F2["释放策略对照实验"]:::box
        F3["负荷敏感性分析·瓶颈标定"]:::box
        F1 --> F2 --> F3
    end

    END["结论与展望"]:::endbox

    S1 --> S2 --> S3 --> S4 --> END
```
